# Specify target ruby version
version="3.1"

packages_production="tzdata"
commands_production="echo OK"

packages_development="nodejs"
commands_development="curl -fsSL https://deb.nodesource.com/setup_lts.x | bash - && apt-get install -y nodejs && npm install -g yarn"
