services:
  production:
    build:
      context: .
      args:
        packages: ${packages}
        commands: ${commands}
        ruby_version: ${ruby_version}
        asdf_version: ${asdf_version}
    image: $image_prefix:$ruby_version
